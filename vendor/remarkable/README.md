# Remarkable

Content copy from `./lib` of remarkable repository. The `cli.js` is depending on `argparse`, the `linkify.js` is depending on `autolinker`. We didn't use these features, so it's safe to ignore the dependencies. 